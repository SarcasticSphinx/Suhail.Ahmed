import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.pexels.com", "backend.nhost.app"], // add any others you use
  },
};

export default nextConfig;
